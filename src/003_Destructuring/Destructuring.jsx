import React from 'react'

const Destructuring = ({name, course, college}) => {
  return (
    <div>
      <h1>{name}</h1>
      <h1>{course}</h1>
      <h1>{college}</h1>
    </div>
  )
}

export default Destructuring
